{
  "version": "1.0.41",
  "channel": "stable",
  "released_at": "2026-07-07",
  "minimumVersion": "1.0.7",
  "backupRequired": true,
  "components": {
    "backend": {
      "image": "bosesanjit/orbitsm-backend:1.0.41"
    },
    "frontend": {
      "image": "bosesanjit/orbitsm-frontend:1.0.41"
    },
    "db": {
      "image": "postgres:15"
    },
    "nginx": {
      "image": "nginx:1.27-alpine"
    }
  },
  "rollback": {
    "backend": "bosesanjit/orbitsm-backend:1.0.40",
    "frontend": "bosesanjit/orbitsm-frontend:1.0.40"
  },
  "tags": {
    "stable": "1.0.41",
    "latest": "1.0.41",
    "rollback": "1.0.40"
  },
  "notes": [
    "Reduces false update-failed messages by allowing longer write operations to finish before timing out.",
    "Keeps action popups for successful and failed user actions, with backend-provided failure reasons.",
    "Limits incident, problem, and change emails to explicitly involved users instead of notifying executives by hierarchy.",
    "Displays audit trail timestamps in the user's local system time.",
    "Shows SLA remaining or overdue text clearly instead of negative hour values.",
    "Closes user search dropdowns after selecting a person so lower fields are not blocked.",
    "Adds separate L1 reviewer and CAB approver fields for changes and enforces the correct approver at each approval step.",
    "Adds /incidents as a supported incident-register route so direct links and post-login redirects no longer land on a 404 page.",
    "Keeps the Update Center current-version observation synchronized with the runtime container version.",
    "Makes the Problem Linked Work owner editable so admins and handlers can correct wrongly assigned problem ownership from the UI.",
    "Makes people search suggestions expand inline instead of floating over lower fields.",
    "Keeps login lockout at 5 failed attempts and 20 minutes, and resets stale failed-attempt counters when the lock window expires.",
    "Confirms fresh installs continue to bootstrap the default admin account as admin / admin unless explicitly changed.",
    "Adds a Recurring Issue badge and recurring signal when a problem has more than one linked incident.",
    "Normalizes problem approval statuses so Approve and close activates for Pending Approval records, including older status variants.",
    "Removes RESOLVED from the incident UI lifecycle, standardizes resolution review on RESOLVE_REQUESTED, converts legacy RESOLVED records to RESOLVE_REQUESTED, and explains that Reopen is active only after CLOSED."
  ],
  "update_type": "patch",
  "requires_database_migration": true,
  "health_check_url": "/health"
}
